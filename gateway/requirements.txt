# FastAPI and server
# fastapi 0.124.0 widens starlette range so resolution picks a CVE-free starlette (>=0.49.1).
fastapi==0.136.3
uvicorn[standard]==0.49.0

# JWT and authentication
# python-jose 3.5.0 fixes PYSEC-2024-232/233 and relaxes pyasn1 to allow the patched 0.6.x.
# PYSEC-2025-185 (DoS in jose.jwe.decrypt) does not affect this codebase — we only sign/verify
# JWTs (jwt.encode/decode), never decrypt JWEs. The CI's pip-audit step ignores that ID; see
# .github/workflows/ci.yml. Revisit if python-jose ships a fix, or migrate to PyJWT.
python-jose[cryptography]==3.5.0
pydantic==2.13.4
pydantic-settings==2.14.1

# SQLite (async + vector search)
aiosqlite>=0.20.0
sqlite-vec>=0.1.6

# Password hashing
bcrypt>=4.1.0

# HTTP client
httpx==0.28.1

# File uploads (FastAPI UploadFile)
python-multipart==0.0.32

# RAG: document parsing
pypdf>=6.10.2
python-docx>=1.2.0

# Web content extraction (full-page retrieval)
trafilatura>=2.0.0

# Environment variables
python-dotenv==1.2.2

# Live monitor (molebie-ai monitor): host metrics probe.
# GPU metrics are opt-in and lazy-imported:
#   - macOS: install `brew install macmon` separately
#   - Linux (NVIDIA): pip install nvidia-ml-py
psutil>=5.9
